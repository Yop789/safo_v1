import { ApiYoutubePipe } from './api-youtube.pipe';

describe('ApiYoutubePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiYoutubePipe();
    expect(pipe).toBeTruthy();
  });
});
